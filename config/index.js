/**
 * @module config
 * 
 * 配置文件
 * server: 服务配置
 *     host => 域名
 *     port => 端口
 * 
 * proxy: 代理配置列表
 */
module.exports = {
	server: {
		host: '127.0.0.1',
		port: 4001
	},

	proxy: {
		host: '127.0.0.1',
		port: 4001
	}
};